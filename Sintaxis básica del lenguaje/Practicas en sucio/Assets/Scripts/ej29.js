/*
const TOTAL_SUELDO = 10;
const SUELDO_MINIMO = 1000;
let sumaSueldos = 0;
let sueldoUsuario = 0;
let contadorSueldos = 0;

// Recogida de datos: 

for(let i = 0; i <= TOTAL_SUELDO; i++) {
    sueldoUsuario = parseInt(prompt("Introduce un sueldo"));
    sumaSueldos = sueldoUsuario + sumaSueldos;
    if(sueldoUsuario > SUELDO_MINIMO ) contadorSueldos++;

};

alert("Suma sueldos:"  + sumaSueldos);
alert("Total sueldos por encima de: " + SUELDO_MINIMO + "€" + ", es de: " + sumaSueldos + "€");

*/