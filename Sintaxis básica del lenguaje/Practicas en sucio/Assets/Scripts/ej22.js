/*

// Declaración de variables: 

let sumaNumeros = 0;
let contadorNumeros = 0;
let numeroUsuario = 0;

// Recoger datos del usuario: 

while(numeroUsuario >= 0) {
    numeroUsuario = parseInt(prompt("Introduce un valor: "));
    sumaNumeros = numeroUsuario + sumaNumeros;
    contadorNumeros++;
};

// Calcular la media de los números introducidos: 

let mediaNumeros = sumaNumeros / contadorNumeros;

// Mostrar el resultado de la media: 

document.writeln("Media de los números: " + mediaNumeros.toFixed(2));
*/